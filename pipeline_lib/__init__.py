"""Shared library for the databricks-elasticsearch-pipelines framework.

Imported by both the offline generator (scripts/gen_jobs.py) and the on-cluster deploy_views
notebook, so the config schema and its validation live in exactly one place.
"""
