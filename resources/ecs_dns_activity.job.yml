# GENERATED by scripts/gen_jobs.py from _pipelines/pipeline_configs/ecs_dns_activity.yml. DO NOT EDIT BY HAND.
# To change this job: edit the config or the generator template, then rerun the generator.
resources:
  jobs:
    index_pipeline_ecs_dns_activity:
      name: '[${bundle.target}] databricks-elasticsearch-pipelines: ecs-dns-activity'
      description: Export pipeline for the ecs-dns-activity Elasticsearch index. Runs the shared notebook
        notebooks/run_index_pipeline.py with this index's config. No cluster block => serverless notebook
        task.
      max_concurrent_runs: 1
      parameters:
      - name: pipeline_mode
        default: batch
      - name: filter_condition
        default: ''
      - name: chunk_size
        default: ''
      - name: require_existing_index
        default: ''
      - name: verify_certs
        default: ''
      - name: streaming_start
        default: new
      schedule:
        quartz_cron_expression: 0 */10 * * * ?
        timezone_id: UTC
        pause_status: ${var.schedule_pause_status}
      tasks:
      - task_key: index_pipeline_ecs_dns_activity
        notebook_task:
          notebook_path: ../notebooks/run_index_pipeline.py
          base_parameters:
            config_name: ecs_dns_activity
            environment: ${var.environment}
            wheel_path: ${var.wheel_path}
            es_host_url: ${var.es_host_url}
            secret_scope_name: ${var.secret_scope_name}
            secret_key_name: ${var.secret_key_name}
            checkpoint_base_path: ${var.checkpoint_base_path}
      permissions:
      - level: CAN_MANAGE_RUN
        group_name: users
