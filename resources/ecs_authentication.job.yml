# GENERATED by scripts/gen_jobs.py from pipeline_definitions/ecs_authentication.yml. DO NOT EDIT BY HAND.
# To change this job: edit the config or the generator template, then rerun the generator.
resources:
  jobs:
    index_pipeline_ecs_authentication:
      name: '[${bundle.target}] databricks-elasticsearch-pipelines: ecs-authentication'
      description: Export pipeline for the ecs-authentication Elasticsearch index. Runs the shared notebook
        notebooks/run_index_pipeline.py with this index's config. No cluster block => serverless notebook
        task.
      parameters:
      - name: pipeline_mode
        default: streaming
      tasks:
      - task_key: index_pipeline_ecs_authentication
        notebook_task:
          notebook_path: ../notebooks/run_index_pipeline.py
          base_parameters:
            config_name: ecs_authentication
            environment: ${var.environment}
            wheel_path: ${var.wheel_path}
      permissions:
      - level: CAN_MANAGE_RUN
        group_name: users
